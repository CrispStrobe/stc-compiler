DEVICE STC12C5A60S2:
  CLOCK 22118400

  PIN led = P1.0 OUTPUT ACTIVE LOW
  PIN pot = P1.3 ANALOG

  WHEN started:
    FOREVER:
      set v to pot
      IF v < 100 THEN:
        set v to 100
      turn on led
      wait v ms
      turn off led
      wait v ms
